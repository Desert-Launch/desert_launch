{
  "name": "desertlaunch-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Desert Launch landing page — static single-file site, no build step",
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "preview": "python3 -m http.server 8080"
  }
}
